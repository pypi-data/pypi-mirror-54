#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['wsgi_listener']

package_data = \
{'': ['*']}

extras_require = \
{'doc': ['sphinx'], 'test': ['pytest']}

setup(name='wsgi_listener',
      version='0.1',
      description='WSGI listener middleware for WSGI Web Applications inspired by the',
      author='Jonny Fuller',
      author_email='mr.waffles@gmail.com',
      url='https://github.com/JonnyWaffles/wsgi-listener',
      packages=packages,
      package_data=package_data,
      extras_require=extras_require,
      python_requires='>=3.5',
     )
