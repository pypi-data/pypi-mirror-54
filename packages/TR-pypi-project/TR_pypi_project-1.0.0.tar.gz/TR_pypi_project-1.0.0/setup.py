from setuptools import find_packages, setup

setup(
    name = 'TR_pypi_project',
    version = '1.0.0',
    include_package_data = True,
    package_dir = {'':'.'},
    description = 'Hello it is printing hello'
)