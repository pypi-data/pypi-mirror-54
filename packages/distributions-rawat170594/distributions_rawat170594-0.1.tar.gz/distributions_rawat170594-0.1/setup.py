from setuptools import setup

setup(name='distributions_rawat170594',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_rawat170594'],
      zip_safe=False)
