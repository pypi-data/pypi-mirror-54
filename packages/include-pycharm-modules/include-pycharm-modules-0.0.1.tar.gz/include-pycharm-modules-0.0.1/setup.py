#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['include_pycharm_modules']

package_data = \
{'': ['*']}

install_requires = \
['beautifulsoup4']

setup(name='include-pycharm-modules',
      version='0.0.1',
      description='Adds any pycharm source folder to the python module search path so you can import them as if they were installed',
      author='KIC',
      author_email='',
      url='https://github.com/KIC/include-pycharm-modules',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      python_requires='>= 3.7',
     )
