# from usim import Pipe, Scope

from ..utility import via_usim, assertion_mode


class TestPipe:
    @via_usim
    async def test_transfer_uncontested(self):
        pass
        # pipe = Pipe(throughput=2)
